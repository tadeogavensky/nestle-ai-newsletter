services:
  postgres:
    image: postgres:16
    platform: linux/arm64
    container_name: nestle_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-nestle}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nestle}
      POSTGRES_DB: ${POSTGRES_DB:-nestle_ai_newsletter_db}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data

  minio-init:
    image: minio/mc:latest
    depends_on:
      - minio
    restart: "no"
    entrypoint: >
      /bin/sh -c "
      until mc alias set local http://minio:9000 ${MINIO_ROOT_USER:-minioadmin} ${MINIO_ROOT_PASSWORD:-minioadmin123}; do
        sleep 2;
      done;
      mc mb --ignore-existing local/${S3_ASSETS_BUCKET:-nestle-ai-newsletter-assets};
      mc mb --ignore-existing local/${S3_FONTS_BUCKET:-nestle-ai-newsletter-fonts};
      mc mb --ignore-existing local/${S3_EXPORTS_BUCKET:-nestle-ai-newsletter-exports};
      "

  assets-seed:
    profiles: ["seed"]
    build:
      context: .
      dockerfile: backend/Dockerfile
    command: pnpm run assets:seed-minio
    restart: "no"
    depends_on:
      - postgres
      - minio
      - minio-init
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-nestle}:${POSTGRES_PASSWORD:-nestle}@postgres:5432/${POSTGRES_DB:-nestle_ai_newsletter_db}
      DIRECT_URL: postgresql://${POSTGRES_USER:-nestle}:${POSTGRES_PASSWORD:-nestle}@postgres:5432/${POSTGRES_DB:-nestle_ai_newsletter_db}
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ASSETS_BUCKET: ${S3_ASSETS_BUCKET:-nestle-ai-newsletter-assets}
      S3_FONTS_BUCKET: ${S3_FONTS_BUCKET:-nestle-ai-newsletter-fonts}
      S3_EXPORTS_BUCKET: ${S3_EXPORTS_BUCKET:-nestle-ai-newsletter-exports}
      S3_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin123}
      S3_FORCE_PATH_STYLE: "true"

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - postgres
      - minio
      - minio-init
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-nestle}:${POSTGRES_PASSWORD:-nestle}@postgres:5432/${POSTGRES_DB:-nestle_ai_newsletter_db}
      DIRECT_URL: postgresql://${POSTGRES_USER:-nestle}:${POSTGRES_PASSWORD:-nestle}@postgres:5432/${POSTGRES_DB:-nestle_ai_newsletter_db}
      CLIENT_ID: ${CLIENT_ID:-}
      CLIENT_SECRET: ${CLIENT_SECRET:-}
      S3_ENDPOINT: http://minio:9000
      S3_REGION: us-east-1
      S3_ASSETS_BUCKET: ${S3_ASSETS_BUCKET:-nestle-ai-newsletter-assets}
      S3_FONTS_BUCKET: ${S3_FONTS_BUCKET:-nestle-ai-newsletter-fonts}
      S3_EXPORTS_BUCKET: ${S3_EXPORTS_BUCKET:-nestle-ai-newsletter-exports}
      S3_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      S3_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin123}
      S3_FORCE_PATH_STYLE: "true"
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  minio_data:
